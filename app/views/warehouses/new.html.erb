<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @warehouses.new_record? ? 'New Warehouse' : 'Edit Warehouse' %></title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f9fafc;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      max-width: 600px;
      margin: 20px;
      padding: 20px;
      background: #fff;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
    }
    h1 {
      color: #333;
      text-align: center;
      margin-bottom: 20px;
    }
    .field, .actions {
      margin-bottom: 20px;
    }
    .field label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
      color: #555;
    }
    .field input[type="text"] {
      width: 100%;
      padding: 10px;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-sizing: border-box;
    }
    .actions input[type="submit"] {
      width: 100%;
      padding: 15px;
      background-color: #007bff;
      border: none;
      border-radius: 5px;
      color: white;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    .actions input[type="submit"]:hover {
      background-color: #0056b3;
    }
    #error_explanation {
      border: 2px solid #ff4c4c;
      padding: 15px;
      background-color: #ffe6e6;
      border-radius: 5px;
      margin-bottom: 20px;
    }
    #error_explanation h2 {
      color: #ff4c4c;
      margin: 0 0 10px;
    }
    #error_explanation ul {
      margin: 0;
      padding: 0;
      list-style: none;
    }
    #error_explanation ul li {
      color: #ff4c4c;
    }
    .back-link {
      display: block;
      margin-top: 20px;
      text-align: center;
      text-decoration: none;
      color: #007bff;
      font-weight: bold;
      transition: color 0.3s;
    }
    .back-link:hover {
      color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= @warehouses.new_record? ? 'New Warehouse' : 'Edit Warehouse' %></h1>
    <%= form_with(model: @warehouses) do |form| %>
      <% if @warehouses.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@warehouses.errors.count, "error") %> prohibited this warehouse from being saved:</h2>
          <ul>
            <% @warehouses.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <div class="field">
        <%= form.label :name %>
        <%= form.text_field :name %>
      </div>
      <div class="field">
        <%= form.label :address %>
        <%= form.text_field :address %>
      </div>
      <div class="actions">
        <%= form.submit 'Save' %>
      </div>
    <% end %>
    <%= link_to 'Back', warehouses_path, class: "back-link" %>
  </div>
</body>
</html>
